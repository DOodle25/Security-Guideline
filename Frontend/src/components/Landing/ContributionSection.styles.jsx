// src/components/ContributionSection.styles.jsx
import styled from "styled-components";
import { Box, Typography } from "@mui/material";

export const ContributionSectionContainer = styled(Box)`
  padding: 4rem 0;
  background-color: white;
  text-align: center;
`;
